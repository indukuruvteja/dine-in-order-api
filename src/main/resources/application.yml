spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dinein?useSSL=false&serverTimezone=UTC
    username: root
    password: Iteja@0009
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  thymeleaf:
    enabled: true
    prefix: classpath:/templates/
    suffix: .html

  web:
    resources:
      static-locations: classpath:/static/

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
